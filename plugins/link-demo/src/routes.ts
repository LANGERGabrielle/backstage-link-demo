import { createRouteRef } from '@backstage/core-plugin-api';

export const groupRouteRef = createRouteRef({
  id: 'link-demo-group',
});

export const websiteRouteRef = createRouteRef({
  id: 'link-demo-website',
});
